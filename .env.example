# Required when the AI pipeline is added.
OPENAI_API_KEY=
OPENAI_CHAT_MODEL=gpt-5.6
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Optional database filename inside ./data. Defaults to recollect.db.
DATABASE_PATH=
